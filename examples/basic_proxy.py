"""Basic proxy usage — the simplest way to use Shear.

Run this example:
    shear proxy

Then point your application at http://localhost:9800 and watch
requests flow through with human-readable summaries.
"""
